// class PaymentProcesser {
//   processPayment(method: String, amount: number): void {
//     if (method === "credit_card") {
//       console.log(`Processing payment of ${amount} through card`);
//     } else if (method === "paypal") {
//       console.log(`Processing payment of ${amount} through paypal`);
//     }
//   }
// }

// const processor = new PaymentProcesser();

// processor.processPayment("paypal", 100);

// processor.processPayment("credit_card", 200);
